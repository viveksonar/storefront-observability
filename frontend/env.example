# Local dev: copy to `.env.local` in this folder, then restart `npm run dev`.
# Talks to uvicorn directly (avoids Vite proxy / port mixups with 8000 vs 8001).
# VITE_API_BASE_URL=http://127.0.0.1:8000
