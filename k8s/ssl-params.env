# Variables for Ingress TLS / cert-manager (loaded by kustomization.yaml).
# Edit these before kubectl apply -k k8s/ or maintain a gitignored copy.
INGRESS_HOST=agoda.viveksonar.in
TLS_SECRET_NAME=storefront-obs-tls
CERT_CLUSTER_ISSUER=letsencrypt-prod
